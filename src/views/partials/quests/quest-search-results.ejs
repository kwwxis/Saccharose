<% for (let id in searchResults) { %>
  <a role="button" class="quest-search-result secondary dispBlock spacer5-bottom textAlignLeft" href="/quests/<%= id %>">
    <strong>ID <%= id %>:&nbsp;</strong>
    <span><%= searchResults[id] %></span>
  </a>
<% } %>
<% if (!searchResults || !Object.keys(searchResults).length) { %>
  <p>No results found.</p>
<% } %>