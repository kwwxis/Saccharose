
<table class="article-table">
  <thead>
  <tr>
    <th>Chapter</th>
    <th>Act</th>
  </tr>
  </thead>
  <tbody>
  <% for (let chapterName of Object.keys(chapterSect)) { %>
    <tr>
      <th>&nbsp;</th>
      <th><%= chapterName %></th>
    </tr>
    <% for (let subChapterName of Object.keys(chapterSect[chapterName])) { %>
      <tr>
        <td>
          <div class="content"><%= subChapterName %></div>
        </td>
        <td>
          <div class="content">
            <% for (let [i, chapter] of chapterSect[chapterName][subChapterName].entries()) { %>
              <a class="secondary textAlignLeft spacer5-bottom valign" role="button" href="/genshin/chapters/<%= chapter.Id %>">
                <% if (chapter.ChapterIcon) { %>
                  <img src="/images/genshin/<%= chapter.ChapterIcon %>.png" class="spacer10-right" loading="lazy" decoding="async"
                   style="width:48px;height:48px;background:#333;border-radius:50%;border:1px solid #fff;"/>
                <% } %>
                <div>
                  <% if (chapter.Summary.ActNumText) { %>
                    <span><%= chapter.Summary.ActNumText %></span>
                    <span>&nbsp;&mdash;&nbsp;</span>
                  <% } %>
                  <strong class="act-name"><%= chapter.Summary.ActName %></strong>
                </div>
              </a>
              <% if (i !== chapterSect[chapterName][subChapterName].length - 1) { %>
                <hr class="spacer5-bottom">
              <% } %>
            <% } %>
          </div>
        </td>
      </tr>
    <% } %>
  <% } %>
  </tbody>
</table>
