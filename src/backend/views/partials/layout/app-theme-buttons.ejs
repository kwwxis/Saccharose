<div class="toggle-theme-buttons alignStretch button-group spacer5-right">
  <button class="secondary <%= prefTernary('isNightmode').isFalsy().then('selected') %>" value="daymode"><%- icon('sun') %></button>
  <button class="secondary <%= prefTernary('isNightmode').isTruthy().then('selected') %>" value="nightmode"><%- icon('moon') %></button>
</div>
